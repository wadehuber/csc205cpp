#include <vector>

// Starter code: implement your functions here.
// (Java had these as static methods on the WeatherAnalyzer class; in C++
// you can write them as free functions above main(), or as static methods
// of a class -- whichever your lab asks for.)

int main() {
    // Test arrays here
    std::vector<int> temperatures = { 75, 71, 67, 68, 77, 85, 92, 96, 98, 94, 87, 79 };
    std::vector<double> precipitation = { 0.0, 1.8, 0.0, 2.7, 0.4, 0.0, 1.2 };

    // Call your functions and print results where appropriate
    (void) temperatures;
    (void) precipitation;

    return 0;
}
