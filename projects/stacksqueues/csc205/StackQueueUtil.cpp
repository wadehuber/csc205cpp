#include "StackQueueUtil.h"

// All three methods are STUBS in the Java starter code -- they are the
// "Programming Project" the student must complete. We preserve the stub
// behavior exactly so the assignment is unchanged.

bool StackQueueUtil::isPalindrome(const std::string& s) {
    (void)s;  // unused for now (stub)
    return false;
}

std::string StackQueueUtil::reverseWords(const std::string& s) {
    return s;
}

bool StackQueueUtil::checkArrays(int min, int max, int arr1[], int arr2[]) {
    (void)min;   // unused for now (stub)
    (void)max;
    (void)arr1;
    (void)arr2;
    return false;
}
