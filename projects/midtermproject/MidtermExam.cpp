// MidtermExam - driver for the CSC205 midterm project starter code.
//
// This is STUDENT STARTER CODE. In the Java original, the entire body of main()
// was commented out: it exercised a "Listaque<Integer>" class that is NOT part
// of the provided starter files (the student is expected to write it). Because
// that class does not exist, the commented-out block is preserved here exactly
// as a comment so the intent is clear, and main() does nothing -- just like the
// Java version, which produced no output.
//
// The provided data structures (FastQueue, FastStack, LinkedList) are still
// converted in the csc205/ folder so they compile and are ready to use.

#include "csc205/FastQueue.h"
#include "csc205/FastStack.h"
#include "csc205/LinkedList.h"

int main() {

    /*
    Listaque<int> everything;   // Java: Listaque<Integer> everything = new Listaque<Integer>();

    int ii = 1;
    do {
        everything.addToFront(ii);
        ii++;
    } while (ii <= 15);

    for (int jj = 0; jj < 2; jj++) {
        everything.removeFirst();
        everything.removeFirst();
        everything.removeLast();
    }

    do {
        everything.addToRear(ii);
        ii++;
    } while (ii <= 25);

    for (int jj = 0; jj < 3; jj++) {
        everything.removeFirst();
        everything.removeLast();
    }

    std::cout << everything.toString() << std::endl;   // Java: System.out.println(everything);
    */

    return 0;
}
