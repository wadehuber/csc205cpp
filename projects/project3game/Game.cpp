#include <iostream>

// In Java this file imported the characters package. In C++ we #include the
// header so the structure is here even though main() does not use it yet.
#include "characters/GameCharacter.h"

int main() {
    std::cout << "Starting game" << std::endl;
    return 0;
}
