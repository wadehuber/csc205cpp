#include "Recursion.h"

// These are stubs, exactly like the Java starter code -- the project asks
// you to fill in the recursive implementations.

int Recursion::fib(int n) {
    (void) n;
    return 0;
}

int Recursion::ackermann(int m, int n) {
    (void) m;
    (void) n;
    return 0;
}

int Recursion::balance(int x, int y) {
    (void) x;
    (void) y;
    return 0;
}

double Recursion::pi_approximation(int n) {
    (void) n;
    return 0.0;
}

bool Recursion::isPalindrome(const std::string& s) {
    (void) s;
    return false;
}

bool Recursion::monotonicallyIncreasing(const std::vector<int>& a) {
    (void) a;
    return false;
}
